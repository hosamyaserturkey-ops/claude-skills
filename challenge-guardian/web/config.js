// Fill these in once after creating your Supabase project
// (Project Settings → API). The anon key is safe to publish: row-level
// security means each logged-in user can only ever see their own data.
window.GUARDIAN_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT.supabase.co",
  SUPABASE_ANON_KEY: "YOUR_ANON_KEY",
};
